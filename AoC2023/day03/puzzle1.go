package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	scanner := bufio.NewScanner(os.Stdin)
	scanner.Scan()
	sl1 := []rune(scanner.Text())
	for i := 
	for scanner.Scan() {
		sl2 := []rune 
	}
}
